# design-sync notes — HomeLib / MagicLib

- Repo shape: Python + Streamlit, no `package.json`, no Storybook, no `dist/` — the converter (`package-build.mjs`) does not apply. The bundle under `ds-bundle/` is hand-authored: tokens from the standalone mockup's `--hl-*` custom properties, the shipped rotunda fragment rendered by `apps.ui.rotunda.build_rotunda_html(CROSSROADS_DOORS, "Ask")`, the standalone prototype as-is, and the seven owner stills from `docs/mockups/` as `guidelines/inspiration/`.
- `package-validate.mjs` was NOT run: it requires `_ds_bundle.js`, `.ds-build-meta.json` and `_ds_sync.json`, none of which exist for a bundle without JS components. Cards were checked by rendering them in a browser instead (see the session's screenshots). No `_ds_sync.json` is uploaded — every future sync re-verifies everything (documented safe state).
- Cards are hand-authored → register them with `register_assets` after `write_files` (the legacy path for projects without a converter build).
- Fonts: Georgia / system-ui only — no `@font-face` needed.
- 2026-09-06: DesignSync refused every call in the headless session ("needs design-system authorization"); the owner must run `/design-login` once in an interactive `claude` terminal on this machine.
- 2026-09-06 ~09:40: `/design-login` done from a terminal; project **MagicLib** created (`fa4ad340-1ab4-4ecb-b2c7-3f1c3cb3d41f`, https://claude.ai/design/p/fa4ad340-1ab4-4ecb-b2c7-3f1c3cb3d41f), incremental path (empty project): 18 files + sentinel uploaded from `ds-bundle/` (tokens, three `@dsCard` previews, seven inspiration stills, conventions README). No `_ds_sync.json` uploaded — the next sync re-verifies and re-uploads everything, by design for a hand-authored bundle.
- Re-sync recipe: `uv run python -c "from apps.ui import rotunda; from apps.ui.view_model import CROSSROADS_DOORS; print(rotunda.build_rotunda_html(list(CROSSROADS_DOORS), CROSSROADS_DOORS[0]))"` for the rotunda fragment; tokens straight from the standalone mockup's `--hl-*` block; then `finalize_plan` with writes `components/**, tokens/**, guidelines/**, styles.css, README.md, _ds_needs_recompile`.
