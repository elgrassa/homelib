# Build evidence log

One row per completed work package. `verify` lists the commands the orchestrator
re-ran itself (subagent claims are never accepted as evidence).

| WP | branch / PR | verify commands run | result | date |
|---|---|---|---|---|
| WP-00 preflight | (local) | `which djvutxt` | **djvulibre GO** — /opt/homebrew/bin/djvutxt (3.5.30); WP-05 is in scope | 2026-08-30 |
| WP-00 preflight | (local) | `docker info` | Docker Desktop 29.7.2 daemon up (was down; started) | 2026-08-30 |
| WP-00 preflight | (local) | OpenAI-SDK function-calling probe vs Ollama `/v1` | **`qwen2.5:7b-instruct` GO** — emitted `search_catalog{"query":"building machine learning systems","subjects":[]}`. Chosen as the compose default (4.7 GB, reviewer-friendly). `gemma4:26b-a4b-it-qat` also GO (no `subjects`), kept as the local heavy override. | 2026-08-30 |
| WP-00 scaffold | feat/scaffold | `just ci` | ruff clean · ruff format clean (13 files) · mypy --strict clean (9 files) · pytest 1 passed, coverage 100% ≥ 90% floor | 2026-08-30 |
| WP-01 specs | feat/scaffold | `ls specs/*.md \| wc -l`; section-structure grep over all 18 | 18 specs, each with the mandated sections. Orchestrator cross-read found ONE contradiction: `answer.md` and `monitoring.md` both consume a `query_log.degraded` flag that `indexing.md` (schema owner) never defined, and monitoring hedged its error-rate panel on "if degraded is logged". Reconciled by adding `degraded boolean NOT NULL DEFAULT false` to the DDL and making the panel unconditional. | 2026-08-30 |
| WP-02 core models | feat/scaffold | `just ci` re-run by orchestrator; read `models.py` + `test_extra_metadata_survives_roundtrip` | 13 tests pass; mypy --strict clean (10 files); ruff clean; coverage 93.94% ≥ 90%. `extra="allow"` on all 6 models, verified by reading the source, and the round-trip test genuinely serializes through JSON text rather than asserting on construction. | 2026-08-30 |
| WP-00 fix | feat/scaffold | `uv run ruff format --check .` | ruff 0.16 formats Python fences inside Markdown, so all 18 specs failed the format gate. Excluded `*.md` from ruff — specs carry illustrative signatures, not runnable code. | 2026-08-30 |
| WP-07 (partial) | feat/scaffold | `docker compose --env-file .env -f docker/docker-compose.yml config` | Compose file, three Dockerfiles, initdb DDL and Grafana provisioning written and structurally valid; 0 unset-variable warnings with `--env-file`. | 2026-08-30 |
| WP-07 defect found | feat/scaffold | `docker compose -f docker/docker-compose.yml config` with a repo-root `.env` present | **Reviewer-path bug, fixed.** Compose treats `docker/` as the project directory and looks for `docker/.env`, so the README's `cp .env.example .env` left `POSTGRES_PASSWORD` and `LLM_MODEL` blank — a warning, not an error, so the stack would have booted misconfigured. All compose invocations now pass `--env-file .env`, and `just` refuses to run without one. | 2026-08-30 |
| WP-07 BLOCKED | feat/scaffold | `docker pull alpine:3.21` (3 min, no progress); `curl https://registry-1.docker.io/v2/` → 401 in 0.39s | **Docker daemon cannot pull images**, though the registry is reachable from the shell — so this is Docker Desktop's own networking, not the network. Blocks image digest pinning (Reproducibility), the compose bring-up, and the cold-clone drill. Images currently carry tags with NO digests. Needs a human fix (restart Docker Desktop / check its VM networking) before Day 3. | 2026-08-30 |
| WP-03 txt/md/epub | feat/scaffold | `just ci` re-run by orchestrator; read `normalize.py` + `test_epub_zip_bomb_guard` | 15 format tests pass. Dispatcher already routes all five extensions, with `pdf.py`/`djvu.py` as raising stubs, so WP-04/WP-05 replace only their own file and can never conflict on the dispatcher. Zip-bomb guard is genuinely behavioural: it asserts `read_epub` is never called once the cap trips. NOTE: the subagent stalled waiting on a background task and never reported its verify output — the work was verified entirely by the orchestrator. | 2026-08-30 |
| WP-06 chunker | feat/scaffold | `just ci` re-run by orchestrator | 16 chunk tests pass; 44 tests total; coverage 93.40% ≥ 90%. The degenerate-input test caught a **real infinite loop**: when a packed window fitted entirely within `overlap`, the backward overlap search regressed the next window's start to the current window's start, so the cursor never advanced. Fixed by bounding the pull-back. | 2026-08-30 |
| WP-04 PDF | feat/scaffold | `uv run pytest packages/homelib-core -v`; `which tesseract` | 15 PDF tests pass (70 in the package). Tesseract 5.5.2 present, so the scanned-PDF tests run **real OCR**, not a mock, while staying skip-guarded. `pdf.py` 97%. Per-page (not per-document) OCR is what makes `method="mixed"` a real outcome. | 2026-08-30 |
| WP-05 DjVu | feat/scaffold | `uv run pytest -k djvu -q`; same with djvulibre off PATH | 21 pass with the binary; 19 pass + 2 clean skips without it — both re-run by the orchestrator. `djvu.py` 100%. Parses per-page S-expressions rather than form-feeds, because a page with no text layer emits `()` and page numbers would otherwise drift. Its own test caught an octal-escape bug (`isdigit()` matches 8/9). | 2026-08-30 |
| WP-12/15 core | feat/scaffold | `just ci`; read `test_mrr_hand_computed` | 109 tests, coverage 96.38%. Metrics reimplemented from definitions; hand-computed tests write out the arithmetic and assert hit-rate ≠ MRR on the same data, so a metric collapsing into the other cannot pass. The gate counts a metric MISSING from the current run as a regression, and refuses to load a baseline whose floors carry no note. | 2026-08-30 |
| WP-07 blocker, ROOT-CAUSED | — | bisected: `docker run` OK · 45 MB downloaded inside the VM in 2.3s · VM disk 149 GB free at 4.7 GB/s · `docker-credential-desktop get` never returns | **The credential helper hangs.** `~/.docker/config.json` has `credsStore: desktop` and a stale, EMPTY `auths` entry for index.docker.io, so every pull calls a helper that never returns — while everything else works. Proved by pulling with a clean `DOCKER_CONFIG`: alpine, pgvector, ollama and grafana all pulled in seconds. Fix for the host: `docker logout`. |
| WP-07 correction | — | see row above | My earlier diagnosis in this log blamed **Tailscale/MTU** (5 `utun` tunnels at MTU 1280/1000, small requests succeeding while large ones stalled). **That was wrong** and is retracted: a 45 MB download inside the VM took 2.3s, so there was never an MTU or bandwidth problem. Recorded because acting on the wrong diagnosis would have meant tearing down working homelab networking. | 2026-08-30 |
| WP-07 digests | feat/scaffold | `docker images --digests`; `docker compose config -q` | All images now pinned by tag **and** digest, read from images actually pulled today: postgres 17.11, ollama 0.33.2, grafana 13.0.2. Version tags verified to resolve to the same digests as `:latest`. | 2026-08-30 |
| WP-08 fetch | feat/scaffold | `fetch_corpus.py --verify-only`; `wc -l data/catalog.jsonl`; coverage probe | 18/18 books verified against real sha256 hashes; 3061 deduped Open Library records, subject coverage 99.97%. **description coverage 0%** — search.json has no description field at all (it lives only on the per-work endpoint). Costs nothing: the roadmap was already specified to generate its own rationale and treat descriptions as optional. | 2026-08-30 |
| WP-07 live | feat/scaffold | `docker compose up -d postgres grafana`; `\dt`; grafana datasource health | **Stack verified running, not just valid.** Both services healthy. 6 tables created, `vector` extension present, generated `tsv` column and both indexes present. `grafana_ro` INSERT → `permission denied` (read-only genuinely enforced). Grafana datasource health → `Database Connection OK`. | 2026-08-30 |
| WP-07 ports | feat/scaffold | `lsof -iTCP:<port> -sTCP:LISTEN` | 11434, 3001 and 8000 were already taken on this machine (host Ollama + two containers). Host ports are now overridable from `.env` with the documented defaults, so a busy machine does not have to edit the compose file to try the project. | 2026-08-30 |
| WP-08 snapshot | feat/scaffold | `uv run python -m apps.ingest.build_snapshot`; invariant sweep over the real corpus | **Live gate caught a critical defect.** First run produced exactly 1 block for all 18 books: Gutenberg ships CRLF, and the heading heuristic compared the next line to `""` while it held `"\r"` — so no heading was ever detected and the citation story silently evaporated. Synthetic LF fixtures passed throughout. Fixed with newline normalisation + 2 regression tests (CRLF and lone-CR). After: 729 blocks with real chapter titles, 9,168 chunks, **0** violations of `canonical_text[start:end] == text` on real data. Snapshot 6.0 MB, sha256 `a279f1c6…`. | 2026-08-30 |
| WP-14 de-risk | (probe) | grounded-answer probe against real corpus chunks via `qwen2.5:7b-instruct` | Model cites correctly by passage index, but **invented an author**: attributed Ford's *My Life and Work* to Theodore Roosevelt. The probe's context carried title + section only. Consequence for WP-14: authors must be passed explicitly in the answer context, and citation validation must check attribution rather than only that the `chunk_id` resolves. | 2026-08-30 |
| Perf budget | (probe) | 3 warm calls after a cold call | Cold start 23.9s; warm 1.4s for 26 completion tokens (~18 tok/s). A 237-token grounded answer costs ~13s warm. Retrieval eval is LLM-free per query so 150–250 questions is fine; **LLM eval must stay bounded** (~30–50 questions × 3 variants × judge) or it will not finish in a sitting. | 2026-08-30 |
| WP-16 dashboard | feat/scaffold | Grafana `/api/dashboards/uid/homelib-overview`; every panel's SQL executed against the live schema | 6 panels provisioned from committed JSON and confirmed loaded by Grafana (not just valid JSON). All 6 panel queries executed against the real database — **0 broken** — with `$__timeFilter` emulated, so a column typo surfaces now rather than during the demo. Panels: queries/hour, latency p50+p95, arm distribution, feedback ratio, token usage, degraded-answer rate. | 2026-08-30 |
| Coverage sweep | feat/scaffold | `pytest --cov` per-module review | Found `apps/ingest/build_snapshot.py` at **0% — code I wrote myself and never tested**. Writing the tests then found two real defects in it (below). Global coverage had masked it at 92%. | 2026-08-30 |
| build_snapshot defects | feat/scaffold | `test_snapshot_is_byte_stable_across_rebuilds`; `test_main_reports_and_exits_zero` | (1) gzip stamps the **output filename** into its header as well as mtime, so the committed snapshot's bytes were not content-determined — `filename=""` now. (2) `books_dir`/`out_path` were bound as import-time defaults, so they could not be reconfigured or tested without touching the real data dir; resolved at call time now. Rebuilt snapshot verified byte-identical across two runs: sha256 `4148f50b…`. | 2026-08-30 |
| WP-13 UI | feat/scaffold | `uv run pytest apps/ui/tests`; `ruff check`/`format --check`; `mypy apps/ui`; scoped `--cov=apps/ui` | 35 tests, **100%** on `api_client.py` + `view_model.py`, mypy clean. A static AST test walks every file under `apps/ui` and fails on any import of `psycopg`/`homelib_core`/`homelib_rag`/`sqlalchemy`/`dlt`, so the API-only boundary is enforced, not just documented. Pure logic was split out of `app.py` into `view_model.py` so the coverage floor stops exempting all of `apps/ui` and exempts only the un-runnable render shell — the previous config silently un-guarded the vote-state and formatting code. Covering the last two branches found the cases worth having: a JSON error body with no `detail` key, and a 204 with no body where `.json()` would raise. | 2026-08-30 |
| Forgejo online | feat/scaffold | `git ls-remote forgejo`; the CI run history | **36 commits are off-machine.** Repo `elgrassa/homelib` created by the owner; remote added, branch pushed. First CI run on a brand-new repo went **green first try** (`ruff + mypy + pytest`, success). `main` seeded from that CI-verified commit and set as default; everything after it goes through a PR. | 2026-08-30 |
| Lint drift caught | feat/scaffold | `ruff check $(git ls-files '*.py')` | An unsorted import block in `evals/tests/test_ground_truth.py` had been committed and would have failed CI. It was invisible because three agents' untracked in-flight files were making plain `ruff check .` noisy — running ruff over **tracked files only** separated my errors from theirs. | 2026-08-30 |
| publish gate defects | feat/scaffold | live query against the CI run history; `just publish` | **Three defects in my own submission gate, all last-day-only.** (1) Forgejo's status enum is 1=success/2=failure/3=cancelled — the recipe treated 3 as green, so a **cancelled** run would have published as verified. (2) The CI database path pointed at a location that does not exist, so every run took the "not readable" branch. (3) It keyed on branch, not commit, and was unscoped across an instance hosting many repos. Enum confirmed empirically: 8,547 rows at 1, and every job under a status=1 run is itself 1. Fixed in PR #1. | 2026-08-30 |
| Duplicate CI run | feat/scaffold | the CI run history for the same commit; a decoded run log | **The duplicate run was the cause of the red, not a separate annoyance.** `on: push` (any branch) + `pull_request` fired two runs of the same sha `c1d09cd` on one host, in distinct concurrency groups so neither cancelled the other. Both DROP/CREATE a fixed `homelib_test_index`, so one deleted the other's database mid-test: one run went red with `database ... does not exist` on 6 tests, the parallel run on the identical tree stayed green. Fixed on both axes — per-process database name, and `push` scoped to `[main]` (a convention this studio already documents elsewhere). Next push produced exactly **one** run, green. | 2026-08-30 |
| Secret scan enforced | feat/scaffold | `gitleaks git`; planted-credential test; `just ci` | `.gitleaks.toml` existed but was wired into nothing. First run: 4 findings, all false positives (3× `"key"` as a MetricSpec field name, 1× the drill's runtime `curl -u`). The path-scoped allowlist that suppresses them **also suppressed a planted GitHub PAT and AWS key id** — verified, not assumed — so the shipped version matches on the strings themselves, and the metric-name entry matches the extracted secret rather than the line. CI checkout moved to `fetch-depth: 0`: on the default shallow clone `gitleaks git` sees one commit and reports clean forever. | 2026-08-30 |
| WP-14 API | feat/scaffold | `ruff check`/`format --check`, `mypy --strict`, 211 tests — all re-run by the orchestrator | 7 endpoints, agent loop, roadmap, grounded answers. The author-hallucination class is closed **structurally**: `_RawCitation` has no `book_title`/`book_id` field, so an invented attribution has no path into a response; titles are read from Postgres. Degradation returns 200 + `degraded: true` for LLM-unreachable, vector-backend-down and failed citation validation. `specs/openapi.snapshot.json` + a drift test pin the contract the UI consumes. | 2026-08-30 |
| WP-09 dlt | feat/scaffold | `uv run pytest apps/ingest/tests` against a live Postgres; `python -m apps.ingest.pipeline` twice | **37 passed, 0 skipped**, 7m51s — re-run by the orchestrator. The zero matters: these tests skip themselves when no database is reachable, and an earlier run reported "1 skipped" purely because Docker had restarted, which reads as green in the summary line. ELT design: dlt owns `homelib_staging`, an explicit ordered transform loads `public` in FK order in one transaction, so a schema-evolving loader never touches the generated `tsv` column or the `vector(384)` type. End state 18/729/9168/9168/3061, identical on a second run. | 2026-08-30 |
| Answer path was fully broken | feat/scaffold | live runs against the real corpus, 3 then 12 ground-truth questions | **The eval harness found it the hour it landed: 3 of 3 questions returned "I couldn't produce a verified answer."** Three defects — the model invented chunk_ids (`10028766648516879691`); verbatim matching was byte-exact against hard-wrapped text; and it quoted real corpus text while mislabelling which passage it came from. Fixes: cite by passage number (the model never names an id), collapse whitespace only, and resolve a quote against every shown passage. **0/3 → 7/12 non-degraded**, 5 hits retrieved every time. The unit suite was green throughout, because every fake produced a perfect id and a byte-exact quote. | 2026-08-30 |
| Compose project name | feat/scaffold | `docker volume ls`; row counts after a Docker restart | A restart brought the stack up on an **empty** volume while the seeded one sat untouched: Compose names the project after the directory holding the compose file, which is `docker/`. Pinned `-p homelib` in the justfile and in the README quickstart, which is the path a reviewer copies. Third instance in this repo of one bug — a constant shared identifier on a shared host (test database name, two CI runs, compose project). | 2026-08-30 |
| PR #1 merged | main | Forgejo PR merge (rebase); `git rev-list --merges` | Merged on green CI. **0 merge commits on main** — linear history holds. CI took 9m34s against a 10-minute cap; the timeout is raised to 25 in the follow-up rather than left at 26 seconds of margin. | 2026-08-30 |
| WP-12b retrieval eval | feat/scaffold | `just eval-retrieval`; matched 80-row rewrite comparison; determinism re-run | **4 arms × 235 questions, 0 degraded across all 940 arm-runs.** lexical 0.072/0.066, vector 0.106/0.092, hybrid 0.174/0.152, hybrid_rerank 0.174/0.167. Fusion is the gain (+64% over the better single arm); rerank lifts MRR and leaves hit-rate flat, which is what a reranker does. **Rewrite measured and rejected** on a matched sample (0.150/0.144 off vs 0.150/0.138 on) — and verified not silently no-opping: it rewrote 6 of 6 sampled questions into keyword form. Eval verified **deterministic** (two runs, identical metrics), so baseline margins are 0.01 rather than a noise-absorbing width. | 2026-08-30 |
| Retrieval quality diagnosed | feat/scaffold | 60-question proximity probe against the live index | 0.174 hit-rate reads alarming, so it was checked rather than reported bare: exact chunk 0.133, **correct book 0.650**, correct section 0.400. Retrieval finds the right book two-thirds of the time across 18 books (chance 5.6%) — the metric is single-positive and chunk-exact, so a neighbouring chunk with equally relevant prose scores as a total miss. Both true: the metric understates usefulness, AND there is real headroom. Recorded in ADR-001 rather than smoothed over. | 2026-08-30 |
| CI lane split | main | Forgejo runs 12292 (cancelled), 12298 (12m in `uv sync`), 12304 (both jobs green) | **Three wrong fixes before I read the log properly.** Raising `timeout-minutes` to 25 could never work: the quick runner's daemon caps jobs at 25m and reports its own kills as **CANCELLED, not failed**, which reads as a hang. Run 12298 then showed where the time actually went — checkout 2s, `uv sync --frozen --all-extras` **11.5 min**, tests never started, because the runner wipes its workdir every job and rebuilds the venv (torch, sentence-transformers) from scratch. Final shape: `gate` on the quick lane (20m, under the daemon cap) for ruff/gitleaks/mypy/fast tests; `full-suite` on the **heavy** lane (both heavy runners carry `heavy`; only one carries `e2e`, which had queued us behind another repo's backlog) for the whole suite plus the coverage floor. | 2026-08-30 |
| Ingest tests 2.6× faster | main | `pytest apps/ingest/tests` before/after | Seven integration tests each called `run_pipeline()`, and every call embeds all 9,168 chunks — seven full embedding passes, **14m36s**, enough on its own to blow the CI cap twice. One session-scoped load answers all of them, and the two idempotency tests now run their second pass over it, which is closer to what they actually claim. **5m38s**, still 37 tests, still 0 skipped. | 2026-08-30 |
| num_ctx truncation — RULED OUT | main | `/api/ps`, `/api/show`, measured prompt sizes | A peer session proposed that Ollama's `/v1` endpoint silently ignoring `options.num_ctx` was truncating prompts and causing the ~40% bare-`{}` answers. Plausible and worth killing properly: at a 2048 window, ~1,900 tokens of prompt would leave almost nothing to generate into, producing exactly that symptom. **Measured: the model is loaded at context 32768**, and real prompts at k=5 run 1,760–1,915 tokens. No truncation is possible. The bare `{}` is the 7B model failing the JSON contract, not a config bug. | 2026-08-30 |
| WP-15 bake-off | main | `just eval-llm` ×4 runs; cross-run variance analysis | 4 arms (3 challengers + a **production control**, imported not copied) × 30 questions. Final run on a quiet box, resident models unchanged across it. `stepwise` won on the judge's overall score — and **the result does not survive the other runs**: it scored 1.90/1.60/1.67/2.07 across four runs of the same code on the same corpus, a spread of 0.47 against a between-arm spread of 0.34, and the winner changed from `concise` to `stepwise` between runs. **Null result: the incumbent stays** (ADR-003). | 2026-08-30 |
| Hedging guard earned itself | main | `evals/results/llm_eval.md`; `test_report_flags_a_winner_that_declined_most_often` | `stepwise` also declined to cite anything more often than any other arm (7/30), so the report marks it **unproven** instead of presenting it as a result. Exactly the predicted failure — a zero-citation answer counts as a success, so an arm that hedges has less for a weak judge to mark down. Without the ungrounded column, `stepwise` would have been crowned and the shipped prompt replaced with one that answers **less often**. Credit to a peer session for catching the flaw before the run, not after. | 2026-08-30 |
| Context window was undeclared local state | main | `docker/docker-compose.yml`; `test_compose_pins_the_ollama_context_window` | My earlier "num_ctx truncation — RULED OUT" was true of this machine and **wrong as a general claim**: the 32768 I measured is one `launchctl setenv` on this Mac, and nothing in the repo carried it. A reviewer would inherit the image default and get silent prompt truncation — Ollama truncates rather than erroring, so the model answers a question it was only shown part of. Now pinned at 16384 in compose, covering the `_MAX_CONTEXT_HITS = 20` ceiling (~7,600 tokens) with ~2× headroom. | 2026-08-30 |
| Cross-session lesson | — | two concurrent automated sessions sharing one machine | A session can **degrade another's instrument by improving its environment**. My harness snapshots resident models at both ends and warns on any change; a peer deliberately did NOT evict 15GB of orphaned model mid-run, because doing so would have tripped that check and made me discard a sound run. Environment changes need announcing, not just making — even the helpful ones. | 2026-08-30 |
| Model bake-off: DO NOT SWAP | main | second session, 12 questions, same hits, judge pinned to qwen | `gemma4:26b-a4b-it-qat` vs the incumbent. Half the hypothesis confirmed: gemma **eliminates the bare `{}` entirely, 4 → 0**. It then trades it for a worse failure — 7 ungrounded declines against qwen's 3, all verbatim "The provided passages do not answer the question", including on questions where **qwen grounded valid citations from the same hits**. Over-refusal, not incapacity. Latency disqualifies it independently: **68.1s median against production's 30s ceiling**, plus two 180s timeouts. Suspected cause worth chasing: `_SYSTEM_PROMPT`'s decline invitation protects faithfulness and plausibly suppresses grounding — a prompt/model interaction none of the four bake-off arms varied. | 2026-08-31 |
| Determinism retracted | main | incumbent re-run, identical inputs | The same session re-ran the **unchanged incumbent** and got **8/12 grounded, then 4/12** — five of twelve questions flipped. Its earlier per-question determinism claim is withdrawn. Consequence for this project: my own **25/30 grounded is one draw, not a stable property**, and must not be quoted as one. It also confirms the ADR-003 null result from a second direction — my four runs spread 0.47 on judge score, theirs 0.34 on grounded rate, same underlying instability. | 2026-08-31 |
| `OLLAMA_MAX_LOADED_MODELS=1` staged | — | `launchctl getenv` → `1`; the Ollama process untouched | Set but **deliberately not activated**: it needs an Ollama restart, and every window checked had another project's CI mid-flight (6 active jobs with llama3.1 resident at the last check). Setting the variable is inert, so the pin can be staged safely and takes effect whenever Ollama next restarts. Note it changes the failure mode rather than removing it — co-residency becomes impossible, so a competing job evicts your model and the next call pays a full reload. **Owner's decision 2026-08-31: let it activate on the next natural Ollama restart** rather than forcing one and breaking another project's in-flight CI. | 2026-08-31 |
| Feedback loop live | main | `POST /v1/ask` → `POST /v1/feedback` → `SELECT FROM query_log` on the running stack | **Verified end to end, not by reading code.** A real ask returned `request_id` `bafaeee2…`, a 👍 was accepted, and the row persisted with `feedback='up'`. Criterion 7 complete: 6 provisioned Grafana panels plus a closed feedback loop. Incidental confirmation of the known rate — that particular answer came back degraded. Also worth recording for the reviewer path: the API is on host port **8010**, not 8000, because 8000 was already taken on this machine; `docker port homelib-api-1` is the way to find it. | 2026-08-31 |
| Drill found a broken headline feature | fix/citation-block-id | `just drill`; `GET /v1/blocks/<id>` against the live API | **The cold-clone drill earned its existence.** It asked 5 real questions of a freshly built stack; one returned a grounded citation that would not resolve. Measured: `/v1/blocks/<block_id>` → 200, `/v1/blocks/<chunk_id>` → 404, and `Citation` carried only `chunk_id`. The UI's "show full source block" button had **never worked**. No unit test could see it — the UI test asserted the derivation returned a chunk_id (it did), the API test asserted `/v1/blocks` worked (it did, with a block_id); each half was right about itself and the join was never exercised. | 2026-08-31 |
| Drill assertion was a coin flip | fix/citation-block-id | first drill run vs. the model's measured degradation rate | The drill asked **one** hardcoded question and required a non-degraded grounded answer, from a model that declines or malforms roughly half the time. It could fail a reproducible stack and pass a broken one by luck. Now asks up to 5, passes on the first grounded one, and reports which attempt succeeded so a degrading system stays visible instead of being hidden by the retry. | 2026-08-31 |
| Drill NOT green — machine exhausted | fix/citation-block-id | 17 concurrent CI jobs; swap 8.2/9.2 GB; Postgres killed mid-query; Docker daemon down | **Stated as an open gap, not a pass.** The re-run did not reproduce the resolution failure, but produced 3 degraded answers and 2 dropped connections — so nothing ever produced a citation to resolve, which is not evidence either way. The drill brings up a SECOND full stack (its own Postgres, Ollama, API, UI); running it twice on a box already saturated by another project's CI contributed to the exhaustion. **Re-run in a quiet window before this ships.** | 2026-08-31 |
| PR #3 merged = verified v1 | main | `git log -1`; ancestry includes `c8f8988` (citation `block_id`) + `467b6bc` (timeout 300) + `535f58b` (eval README) | **HEAD `535f58b1a47d3545c1e4b6a36e75c06b58f5a640`.** Forgejo CI was green on this merge (plan §2). Never rewrite `main`. | 2026-09-01 |
| `v1-fallback` tagged | main | `git rev-parse 'v1-fallback^{}'`; `git ls-remote forgejo refs/tags/v1-fallback` | Annotated tag already present at this SHA (not moved). Peeled commit **`535f58b1a47d3545c1e4b6a36e75c06b58f5a640`**. Tag object `4d642cae1722ef14a31a0dcb8fbd8dfbd27dae07`. Local and `forgejo` refs match. | 2026-09-01 |
| Fallback eligibility | WP00 | course no-reuse rule vs git history of this attempt | The no-reuse rule bans previously *submitted* capstones. v1 was built entirely within this attempt and **never submitted**. Eligible. | 2026-09-01 |
| v2 SOT docs committed | docs/v2-plan | `ls specs/product.md docs/plan-v2.md` | Product SOT = `specs/product.md` (Downloads v2 plan + owner commercial appendix). Execution SOT = `docs/plan-v2.md` **verbatim** (weekdays not silently fixed). | 2026-09-01 |
| Late-deploy (no early canary) | WP00 / ADR-006 | owner 2026-09-01 | GitHub public repo + Streamlit Community Cloud are created **at the end** (submission day). Agents do not create them. Rehearse `APP_MODE=demo` locally. ONNX-vs-torch vs documented Community Cloud memory limits, not a live canary. This PR is config skeleton only — **no FTS5 demo smoke**. | 2026-09-01 |
| Calendar weekday discrepancy | WP00 | `date`; `cal 9 2026` | Machine clock **Tue Sep 1 2026 CEST**. `cal 9 2026`: Sep 1 = **Tuesday**, Sep 6 = Sunday, Sep 7 = **Monday**, Sep 8 = **Tuesday**. `docs/plan-v2.md` still labels Sep 1 = Monday / Sep 7 = Sunday / Sep 8 = Monday 01:00 (the +1 “correction” is still wrong). **Do not silently rewrite the plan.** Next session must not plan a Sunday submission that is actually Monday. | 2026-09-01 |
| Deadline TZ unconfirmed | WP00 | owner banner | Cohort platform TZ for “01:00” still unconfirmed. Treat 01:00 as earliest plausible TZ. Plan operational target remains **Mon Sep 7 20:00 CET** (submission day per `cal 9 2026`) **until owner confirms** — `docs/plan-v2.md` still labels Sep 7 “Sunday” (verbatim freeze). | 2026-09-01 |
| Commercial-split pivot | ADR-010 | owner 2026-09-01 evening | **Supersedes** a separate `homelib-commercial-private` remote this week. One Forgejo repo (`elgrassa/homelib`) until public GitHub; paid tier lands in the **same** Forgejo copy after it is private. `docs/plan-v2.md` §9 still names the two-repo split (verbatim freeze). Do not resurrect a second remote. | 2026-09-01 |
| Course `project.md` sha256 | WP00 | `curl` raw.githubusercontent.com DataTalksClub/llm-zoomcamp `project.md` | **`fde0647a0df91470234593854c619307bf07aa28062d4e090065a57e348c896b`** (matches plan WP11 prefix `fde0647a…`). Re-check at WP11 before submit. | 2026-09-01 |
| Mockups + HTML prototype | docs/mockups | md5 dedupe of owner stills | Eight distinct frames + standalone HTML sha256 `693888a9d320b1479d7adf6eb103ce56db9eb805894cbfbc66f5a3d6dc678869`. Voice mockup = Coming soon (ADR-009). Topology Apple FM = metaphor (ADR-006). | 2026-09-01 |
| WP01 specs | feat/wp01-specs | `ls specs/*.md`; contradiction pass vs product.md | **New v2 files:** principals, editions, provider, coffee-table, progress, scene-search, observatory, rights, connectors, client, rotunda, projection, audio (13). OpenAPI snapshot **unchanged** (v1 live API; first API PR regenerates). Contradiction pass: Grafana→Observatory (ADR-005); Postgres→SQLite target (ADR-004, compose still Postgres); `/v1/roadmap` vs intake+paths; `/v1/books` vs resources; timeout 90 vs 300; snapshot-vs-§8 (docs this WP). Paid-tier tables not added. v1 `degraded` flag still present. | 2026-09-01 |
| WP01 review fixes | feat/wp01-specs | `just ci`; contradiction re-pass | Blockers: `areas.principal_id` on mutating rows; `bookmarks` table; `extra=forbid`/`raw_json` vs ingest `extra=allow` scoped in core-models; `LLM_MAX_OUTPUT_TOKENS=800` canonical + 1600 path ceiling. Majors: product §12.2 canary VOID cross-ref (addendum §2); `listen_progress` may persist rows but no player (ADR-009); ADR-006 calendar cross-ref; `read_progress`/`char_offset` aligned; `PathStepPreview` defined; deadline wording fixed (Mon Sep 7). OpenAPI snapshot unchanged. | 2026-09-01 |
| WP01 review nits | feat/wp01-specs | `just ci` | PR #5 Pavlo review nits closed @ `6ac26b3`. | 2026-09-02 |
| WP02 SQLite | v2 (merged PR #6) | `uv run pytest apps/store/tests/test_sqlite.py -q`; `just ci` | Merged to `forgejo/v2` @ `675995d`. 28 tests green. | 2026-09-03 |
| WP03 ingest | v2 (merged PR #7) | `uv run pytest apps/ingest/tests/test_sqlite_ingest.py -q`; `just ci` | Merged to `forgejo/v2` @ `16269a9`. Counts 18/729/9168/9168; float32 BLOB embeddings; ADR-008. | 2026-09-03 |
| WP04 retrieval | v2 (merged PR #8) + feat/wp04-eval-sqlite | `run_sqlite_pipeline` seed; `HOMELIB_SQLITE_PATH=… just eval-retrieval`; named index reds | PR #8 @ `8653148`. Eval: 4 arms × 235 Q, 0 degraded; winner `hybrid_rerank` **0.638 / 0.572**. Fixed BLOB decode + FTS stopwords (tests `test_load_matrix_accepts_float32_blob_embeddings`, `test_fts_query_drops_english_stopwords_like_plainto_tsquery`). ADR-001 §v2 + baseline floors updated. | 2026-09-03 |
| WP05 catalog | v2 (merged PR #9) | `uv run pytest packages/homelib-rag/tests/test_connectors.py -v` | Merged to `forgejo/v2` @ `bda328a`. Fixture federation; named connector reds. Wiki under `docs/wiki/`. | 2026-09-03 |
| WP06 mentor | v2 (merged PR #10) | `uv run pytest packages/homelib-rag/tests/test_mentor.py -v` | Merged to `forgejo/v2` @ `39b9146`. Intake proposals; high-stakes notice; abstention. | 2026-09-03 |
| WP04/06 local verify | feat/wp04-eval-sqlite | `APP_MODE=demo` refuse unknown; 102 v2 module tests; seed counts; eval-retrieval | Docker Desktop hung on this host — Compose UI/API not re-verified here. Ollama reachable (`qwen2.5:7b-instruct`); API ports 8000/8010/8501 down. Module + SQLite path green. | 2026-09-03 |
| WP07–10 thin surface | feat/v2-wp08-wp11-surface | `uv run pytest apps/store/tests/test_coffee_table.py apps/api/tests/test_v2_routes.py apps/ui/tests/test_view_model.py apps/ui/tests/test_client_conformance.py -q`; `just ci` | P0: `HOMELIB_SQLITE_PATH` wires health/books/ask log + block fetch + feedback. WP07 Coffee Table §5.6 + playlists/progress. WP08 Crossroads doors + HomelibClient InProcess/Http conformance + AST boundary (incl. sqlite3/store). WP09 one-page projection. WP10 Observatory ≥5 charts + `demo_traffic.py --n 40` + feedback UI→DB. OpenAPI snapshot regenerated. WP11: drill/publish/Cloud still owner Mon. | 2026-09-04 |
| WP11 drill status | feat/v2-wp08-wp11-surface | host `uptime` load ~2.7–9.7; `just ci` green (609 passed, 90.64%) | Pre-drill note (superseded by row below). | 2026-09-04 |
| WP11 `just drill` PASSED | `v2` @ `d6f9946` | `just drill` → `/tmp/homelib-drill-run.log`; exit 0 in ~9 min | Cold clone @ `d6f9946`; snapshot sha256 `4148f50b…`; seed books/blocks/chunks/embeddings 18/729/9168/9168; grounded ask attempt **1/5**, arm=`hybrid_rerank`, citation resolves (Wealth of Nations · OF THE DIVISION OF LABOUR); Grafana **6** panels. Compose fleet `homelib-{api,ui,grafana,postgres,ollama}-1` healthy; `:8010/health` 200. Residual Mon: `just publish` / Cloud / submit / peer×3. | 2026-09-04 |
| Compose smoke after Docker recovery | v2 @ `39913df` | `just down && just up && just seed`; curl `:8010/health`, ask, feedback, `:8501` | Docker recovered (logout + restart Desktop). Health/ask/feedback/UI PASS on Postgres path (API port **8010**). First LLM call cold-start can exceed 150s. | 2026-09-03 |
| UI PYTHONPATH false-healthy | v2 @ `3933aa4` (local uncommitted) | rebuild `ui`; curl `_stcore/health`; playwright Crossroads; `pytest tests/test_repo_hygiene.py` | **Root cause:** `streamlit run apps/ui/app.py` without `PYTHONPATH=/app` → `ModuleNotFoundError: apps` while health stayed 200. Fix: `ENV PYTHONPATH=/app` in `docker/ui.Dockerfile` + hygiene pin test. Live: all 6 Crossroads doors render; API `/health` ok (18/9168); seed exit 0. | 2026-09-04 |
| PR-A H1/H3/H2 SQLite-only smoke | `fix/h1-h3-h2-sqlite-demo-path` @ `9e280cd` (PR #25 → `v2`) | `HOMELIB_SQLITE_PATH=data/homelib.sqlite LLM_BASE_URL=http://localhost:11434/v1 bash scripts/sqlite_only_smoke.sh` (API in-process, `DATABASE_URL` unset, `APP_MODE=demo`) | **PASSED.** health `ok` 18/9168; ask non-degraded, arm `hybrid_rerank`, 1 citation resolving via `/v1/blocks/{block_id}` (Walden); Coffee Table 1 item with `X-Demo-Session`, 0 without — demo principal persists. Before the fix every ask on a SQLite-only host was degraded ("failed to load book metadata") and the demo Coffee Table was always empty. | 2026-09-05 |
| PR-A gate | `fix/h1-h3-h2-sqlite-demo-path` @ `9e280cd` | `just ci` ×2 (second after the last edit) | 649 passed, coverage 90.34% ≥ 90%; ruff/format/mypy --strict/gitleaks clean. Regression tests named per finding in the PR body. | 2026-09-05 |
| PR-A follow-up gate + push | `fix/h1-h3-h2-sqlite-demo-path` @ `ae83d51` (PR #25 → `v2`) | `just ci`; `git push forgejo fix/h1-h3-h2-sqlite-demo-path` | Seed one-shots now `--build` the ingest image (the first drill had run a five-day-old image). 650 passed, 90.34%. The Forgejo CI run on `9e280cd` had stalled after checkout until the quick lane's 20-min timeout under host load >400 — infra; the follow-up push re-triggers CI. | 2026-09-05 |
| PR-B gate (restacked on `ae83d51`) | `fix/roadmap-door-monitoring-honesty` @ `c2675e3` (PR #26) | rebase; `uv run pytest tests/test_repo_hygiene.py apps/ui/tests/test_view_model.py apps/ui/tests/test_app_doors.py`; `just ci`; `git push --force-with-lease` (restack, plan addendum §5) | One conflict (both PRs appended to the hygiene test file; both tests kept). 59 targeted green; `just ci` 661 passed, 90.39%. | 2026-09-05 |
| `just drill` re-run — **FAILED at the ask step** | cold clone @ `ae83d51` (project `homelib_drill`, offset ports) | `bash scripts/cold_clone_drill.sh` (scratch log) | Clone from `.env.example`, committed data present, all services healthy, **Postgres seed and SQLite seed with `--build`, `/health` 18/9168 — all PASSED** (confirms the stale-image fix). Ask step: 1 timeout · 2 timeout · 3 answered but cited nothing · 4 timeout · 5 degraded (`hybrid_rerank`) against the 300 s client timeout, host load 340–410 (two `just ci` runs, Forgejo lanes and the drill's CPU Ollama at once). No new code on the request path is implicated; the PASS on `d6f9946` stands as the last green drill. **Quiet-box re-run required before GO/NO-GO.** | 2026-09-05 |
| PR-D rotunda — browser verification | `feat/crossroads-rotunda` @ `ee0f318` (stacked on PR-B) | `streamlit run apps/ui/app.py` on :8601 (`APP_MODE=selfhosted`, `API_URL=http://127.0.0.1:8010`), Chrome; `uv run pytest apps/ui/tests/test_rotunda.py apps/ui/tests/test_app_doors.py` | The iframe route (`st.iframe`; `components.v1.html` is deprecated in 1.62) **cannot navigate**: the sandbox lacks `allow-top-navigation`, so `target=_parent` and `window.parent.location` raise `SecurityError`. Rendered inline with `st.html(unsafe_allow_javascript=True)`; DOMPurify's SAFE_FOR_XML then dropped the whole script because a comment contained `<name` — fixed (no `<` in script text, JSON escapes `<`) and pinned by `test_rotunda_script_text_contains_no_markup_like_characters`. Verified in Chrome: seven doors, Turn the room rotates, **Enter → reload on `?door=Mentor` → "Open door: Mentor"**, grid click → "Open door: Roadmap" with the room agreeing in the same run. Screenshots in `docs/screenshots/`. | 2026-09-05 |
| PR-D gate | `feat/crossroads-rotunda` @ `ee0f318` (stacked on PR-B) | `just ci` in a separate worktree (venv synced with `uv sync --all-extras --all-groups`) | 675 passed, coverage 90.45% ≥ 90%; ruff/format/mypy --strict/gitleaks clean. Screenshots (before: six-door grid on the `v2` tip; after: rotunda + seven doors, Observatory) committed under `docs/screenshots/` in this PR. | 2026-09-05 |
| SQLite-only smoke on the PR-D tree | `feat/crossroads-rotunda` @ `ee0f318` | `HOMELIB_SQLITE_PATH=<abs>/data/homelib.sqlite LLM_BASE_URL=http://localhost:11434/v1 LLM_MODEL=qwen2.5:7b-instruct bash scripts/sqlite_only_smoke.sh` | **PASSED** under host load ~200: health `ok` 18/9168; ask "Who wrote Walden?" non-degraded, `hybrid_rerank`, 35.9 s, answer "Henry David Thoreau", citation resolves (Walden, block `25a30321b30d03cc`); Coffee Table 1 item with the demo header / 0 without. Same host, same hour, the compose stack's old-image `/v1/ask` (Postgres path) degraded after 358 s — the load, not the SQLite path, is what the failed drill measured. | 2026-09-05 |
| Forgejo CI lanes on the stack (host load 200–410 all evening) | PR #25 `ae83d51` · PR #26 `c2675e3` · PR #27 `ee0f318` | the CI run history and decoded task logs | **Quick lane (`ruff + mypy + pytest`, `timeout-minutes: 20`) timed out on both #25 and #26** after ruff, format and gitleaks had passed (`context deadline exceeded … signal: killed` at 17:48→18:07 and 18:01→18:20). **Heavy lane (`full suite + coverage floor`) PASSED on #25 in 44 min** — inside its 45-min limit by one minute; #26's heavy lane and #27's lanes were still running at hand-off. Infra, not code: every local `just ci` on the same commits is green. Owner call: re-run the quick lanes on a quiet box, or raise the quick lane's limit — not changed in this stack (CI edits out of scope, issue #24). | 2026-09-05 |
| PR-C gate | `docs/stakeholder-review-wiki-mermaid-sync` (stacked on PR-D #27) | `just ci`; local mermaid 11 parse+render of all 23 blocks | 678 passed, coverage 90.45% ≥ 90%; ruff/format/mypy --strict/gitleaks clean; 23/23 mermaid blocks render (one state-diagram `;` separator fixed). | 2026-09-05 |
| `just drill` re-run #2 — **PASSED** | cold clone @ `ee0f318` (PR-D head = train tip minus docs; project `homelib_drill`, offset ports) | `nohup bash scripts/cold_clone_drill.sh <scratch>` from the PR-D worktree; log `drill-2.log` | Start 20:53, verdict 22:01 (~68 min, host load 200–440). Clone from `.env.example`; committed data present; all services healthy; **`--build` Postgres seed (18 books)**; **`--build` SQLite seed** `{books: 18, blocks: 729, chunks: 9168, chunk_embeddings: 9168, catalog: 3061}`; `/health` status=ok books=18 chunks=9168; ask: attempt 1 `request failed: timed out` (300 s), **attempt 2 grounded, degraded=False, arm=hybrid_rerank**, citation resolves; Observatory 6 charts, 5 populated, `queries_over_time` 1 point; `✓ DRILL PASSED — pinned commit ee0f318e4c0ea5fb076652f638942e0ed2b87468`; teardown clean. Supersedes the FAILED row above for reproducibility on the train tip; `v2` @ `86ba349` itself has not been drilled since `d6f9946`. | 2026-09-05 |
| Picky review of the train (Phase 2, owner: "verify picky all PRs") | #25 `ae83d51`, #26 `c2675e3`, #27 `ee0f318`, #28 `51747c5`, #29 `2b97f8f`, each diffed against its parent car | `git diff-tree -p <parent>..<head>` per car; cold read + Pass A/8/9; `security-review` co-read on the demo-session + bridge surfaces | **#25 MED** — `ApiClient` re-mints a demo session on 401 but the UI never wrote the new id back to `st.session_state`, so every rerun after a TTL expiry re-minted (fixed on PR-B2 as `persist_demo_session`, test `test_persist_demo_session_keeps_a_reminted_id_for_the_next_rerun`, to keep the gated SHAs). **#26 clean. #27 clean** (accepted: `unsafe_allow_javascript` on constant HTML only; space in `?door=Coffee Table` round-trips). **#28** numeric claims re-derived against this file and `evals/results/retrieval.md`. **#29 clean** (LOW: CLAUDE.md still says "agents do not merge" — true again after this train). Mutation testing: **not wired in this repo, not run.** | 2026-09-06 |
| PR-B2 rehearsal (`APP_MODE=demo`, cold start, no key) | `feat/streamlit-cloud-groq-files` | seed removed from `HOMELIB_SQLITE_PATH`; `uv run streamlit run streamlit_app.py --server.port 8798`; browser + Playwright walk; `ps -o rss` | Seed inflated once at cold start (58.4 MB from the committed 28.7 MB `.gz`, counts 18/729/9168/9168); seven doors + rotunda render through the fixed in-process bridge; demo session minted and persisted; ask with blank `LLM_API_KEY` and no `GROQ_API_KEY` → **explicit degraded card in 27 s, no 500** (`OpenAIClient` used to raise `Missing credentials` on the empty string — fixed, blank = unset, Groq fallback when only `GROQ_API_KEY` is set). RSS ~60 MB before the first run → **~160 MB steady after the ask** (Streamlit child), host load 300–1000. **Not rehearsed with a real Groq key** (none in this checkout). | 2026-09-06 |
| PR-B2 gate | `feat/streamlit-cloud-groq-files` (stacked on #29 `2b97f8f`) | `just ci` ×2 (host load 300–1000) | Run 1: **2 failed / 685 passed, 90.57%** — both `require_sqlite_path` tests, because the gate's repo-local pytest basetemp counted as "inside the repo" and the inflater wrote the seed there; guard narrowed to `data/` only, pin extended. Run 2: **693 passed / 0 failed, coverage 90.59%** (ruff, ruff format, mypy --strict clean; 8 min once the box quietened) | 2026-09-06 |
| Forgejo lanes green on every train head | #25 `ae83d51` · #26 `c2675e3` · #27 `ee0f318` · #28 `51747c5` · #29 `2b97f8f` · #30 `02a26ba` | no re-run API exists, so each PR was closed and reopened via `PATCH /pulls/{n}` (a `pull_request` event re-runs the same SHA); host load 40–180 by then | **quick + heavy lanes ok on all six heads** (graph-guard ok on #29/#30). Earlier reds on these SHAs were all `context deadline exceeded … signal: killed` at the 20/45-min limits under load 300–1000, never a test failure; a re-run on the pre-docs #28 head `4e945e7` had already been green on both lanes. | 2026-09-06 |
| SQLite-only smoke on the train tip | `02a26ba` (= PR-B2 head) | `HOMELIB_SQLITE_PATH=data/homelib.sqlite bash scripts/sqlite_only_smoke.sh` (Ollama `qwen2.5:7b-instruct`, load ~150) | **✓ SQLITE-ONLY SMOKE PASSED** — health ok 18/9168; ask not degraded, `hybrid_rerank`, 254 s under load, 1 citation → 'Walden, and On The Duty Of Civil Disobedience' block resolves; Coffee Table 1 item with the demo header, 0 without. A first attempt hung ~1 h with no output after model load (killed; not reproduced on the rerun). | 2026-09-06 |
| **Train merged into `v2`** | #25 → #26 → #27 → #28 → #29 → #30, `POST /pulls/{n}/merge` `Do: fast-forward-only` with `head_commit_id`, each later car retargeted to `v2` first; #29 un-WIPped | `git log --format='%h %p' 86ba349..forgejo/v2` (single parent every commit); `git merge-base --is-ancestor <car> forgejo/v2` ×6; `git ls-remote forgejo v2` after each merge | **`v2` = `02a26ba`**, linear, no merge commits; all six PRs `merged=True`; the six branches deleted on Forgejo. Post-merge `push` runs 13214–13218 were cancelled by the next push (v2 concurrency), 13219 on `02a26ba` ran. **Found:** `graph-refresh` on 13219 printed "graph is fresh" and pushed nothing — its `git diff -- graphify-out/graph.json` test is empty while `graphify-out/` is still untracked, so the bootstrap graph never lands (follow-up PR, not part of the train). | 2026-09-06 |
| **#31 merged, first graph committed, `main` = `v2`** | 2026-09-06 (owner: merge once green, then fast-forward `main`). `ci/graph-refresh-bootstrap` merged fast-forward-only into `v2` after every CI lane (quick, heavy, graph-guard) passed; hygiene tests re-run locally on that head (29 passed). The post-merge `graph-refresh` job then committed the first `graphify-out/` graph (191 files, ~11.7 MB tracked, `cache/ast/` included) as `eb4a4ff` — a bot push that creates no CI run of its own. `main` fast-forwarded from `e288f21` (v1) to `eb4a4ff`; the first push-triggered CI run on `main` failed for infra reasons unrelated to the code — every job on the CI host died with a filesystem permission error on the runner workdirs; the runners were restarted and later runs proceeded, and the docs push below re-ran both lanes cleanly on the same tree. The bootstrap branch was deleted. Protocol going forward: push to `v2`, wait for the bot's graph commit, then fast-forward `main` — otherwise each branch grows its own graph commit and they diverge. | `git ls-remote forgejo main v2` (both `eb4a4ff`); `git log --format='%h %p' -3 forgejo/main` single-parent | this row |
| **#32 merged (MagicLib design-sync inputs), bot graph, `main` re-synced** | 2026-09-06 (owner: `/design-sync MagicLib` + add the inspiration images as project references). PR #32 `chore/design-sync-magiclib` (`.design-sync/{config.json,conventions.md,NOTES.md}`, `.gitignore` `ds-bundle/`; the bundle itself is uploaded, not tracked): green on every lane (graph-guard, quick, heavy) after a rebase onto the `v2` tip, merged fast-forward-only into `v2`; the push run's `graph-refresh` then committed the updated graph. `main` fast-forwarded to the `v2` tip after this docs commit's own bot refresh (protocol in CHECKLIST §E). The referenced design project holds tokens, the shipped rotunda fragment, the standalone mockup and the seven inspiration stills (19 files incl. sentinel). |
| **graph-refresh ping-pong found and fixed** | 2026-09-06 ~11:30. After the `main` fast-forward to `03242aa`, `main`'s own push run (13318) committed a second graph `808d3b8` although nothing but `graphify-out/` had changed: the freshness check ignores only `built_at_commit`, but each rebuild also moves community ids in `graph.json`, checkout mtimes in `manifest.json` and absolute runner paths in `cache/stat-index.json`, so every push to either branch commits and the branches alternate being one bot commit ahead. `v2` fast-forwarded to `808d3b8`; PR `ci/graph-refresh-v2-only` makes the job exit on `main` (refresh on `v2` only, `main` = pure fast-forward of `v2`), pinned by `test_ci_graph_refresh_skips_main_so_main_stays_a_fast_forward_of_v2` and `test_ci_graph_refresh_still_commits_on_v2` (both run the job script in a scratch repo with a stub `graphify`). Residual, not fixed here: `graphify-out/cache/stat-index.json` stores absolute runner paths and `graphify-out/<date>/` snapshots accumulate — a follow-up on what the bot tracks. |
| **`v2` collapsed into `main`** | 2026-09-06 (owner: "collapse v2 into main, open the PR"). `main` = `v2` = `8a0c6fc`, nothing to merge — the collapse is a CI/docs change: `ci.yml` `push.branches: [main]`, `graph-refresh` case `main) ;;` only (a recreated `v2` falls to the warn-and-exit branch), `post-merge-cleanup` `PROTECTED_BRANCHES: main`, `justfile`/`CLAUDE.md` wording; hygiene tests updated: `test_ci_does_not_double_trigger_on_branch_push_and_a_pull_request` expects `[main]`, `test_ci_graph_refresh_pushes_to_current_protected_branch` asserts no `v2)` case, behavioural `test_ci_graph_refresh_commits_on_main` + `test_ci_graph_refresh_refuses_the_retired_v2_ref` replace the two v2-era tests. | targeted pytest on `tests/test_repo_hygiene.py` (see PR); `v2` branch deletion on Forgejo is the post-merge step | PR `ci/collapse-v2-into-main` → `main` |
| **Redeploy of `main` + live feature pass** | 2026-09-06 (owner: "ensure latest main redeployed locally and verify picky all features"). Stack rebuilt from `main` = `94eae69` (graph tip `99428a5`): `just up` with `--build` → `homelib-api` / `homelib-ui` images 14:35, `homelib-ingest` 14:40, project `homelib`, Postgres + Ollama volumes kept. `just seed` → Postgres 9168 chunks / 9168 embeddings / 3,061 catalog rows (exit 0); `just seed-sqlite` → SQLite 18 books / 729 blocks / 9168 chunks / 9168 embeddings / 3,061 catalog (exit 0). `/health` `ok` 18/9168, LLM `ollama qwen2.5:7b-instruct` reachable. UI in a browser: 7 doors, rotunda rotates and Enter opens the door, Shelf 18·729·9168, Observatory 6 charts populated, Projection loads a book, Coffee Table empty. API pass (scripted, `localhost:8010`): ask attempts 1–4 timed out at 330 s (host load 244–360 from other repos' CI + the Docker VM), attempt 5 grounded `hybrid_rerank`, 3 citations, 71 s, `request_id 3c7b54c4-…`, first citation *Self Help* · MEN OF BUSINESS · block `e75bcb79ba27f410` resolves via `GET /v1/blocks`; feedback 👍 200; Observatory `queries_over_time` 10 → 15; shelf 18/18; Coffee Table add → re-fetch present → progress `read` 200 → delete → not active; mentor intake 200 (84 s, `proposed_path` with 1 step); `POST /v1/paths` with those steps 200 `accepted: true` (a first 422 was the script posting an empty step list — script bug, not product); roadmap 200 (130 s, Open Library steps); scene search keyword 200; audio capabilities `can_generate=False`; demo session mint 200; 18 books. Forgejo on `94eae69`: the PR's CI run green on every lane; the push's quick lane was killed by the 20-min limit under load (ruff/mypy had already passed — infra, not code), heavy lane + graph-refresh green. | `docker images`, `curl /health`, the seed logs, the browser walk, the scripted pass log (5 ask attempts, 18 checks), the CI run history | this row |
| **MagicLib LAN + Projection (review fixes)** | 2026-09-06 `feat/magiclib-lan-projector` review pass. Projector mode exits cleanly (`?projection=` consumed once); shelf ordinal keyed per book; Projection defaults to **This shelf**; Official preview is publisher links only unless `HOMELIB_OFFICIAL_VIEWER=1` (then `:8502/pdf` + `/book` two-page viewer, LAN opt-in); read server: exact-host https allowlist, no redirect following, 64 MiB streamed cap, no CORS wildcard, `no-store` + `nosniff`, book-id allowlist after percent-decoding, upstream errors → 404/502; UI healthcheck probes `:8501` and `:8502`. 7 Pottermore metadata URLs, no PDF bytes in repo. | `uv run pytest apps/ui/tests apps/api/tests tests/test_repo_hygiene.py packages/homelib-rag/tests/test_agent.py -q` → 277 passed | this row |

---

## Plan addendum — post-merge corrections (2026-09-01)

Authoritative until `docs/plan-v2.md` is corrected in the WP01 docs PR. Do **not**
silently rewrite `docs/plan-v2.md` (verbatim freeze); record deltas here and in
specs/CHECKLIST instead.

| # | Correction | Supersedes in `docs/plan-v2.md` | Where recorded |
|---|---|---|---|
| 1 | **Weekdays:** Sep 1 2026 = **Tuesday**; Sep 7 = **Monday** (submission day); deadline night Mon Sep 7 → Tue Sep 8 01:00. Retract "Sunday Sep 7 submission / no Monday build day". Scope-cut Sat Sep 5 18:00; GO/NO-GO Sun Sep 6 18:00 unchanged. | ⏰ banner, §8 calendar labels | table row "Calendar weekday discrepancy"; this section |
| 2 | **§8 Sep 2 gate** VOID "canary live / canary loads publicly" → **local `APP_MODE=demo` smoke green; SQLite tests green**. | §8 Sep 2 row | this section; CHECKLIST §F |
| 3 | **WP04** VOID "ONNX-vs-torch measured on deployed canary" → measured locally under `APP_MODE=demo` (RSS) vs Community Cloud documented limits; no live canary until owner deploys Mon Sep 7. | WP04 verify line | table row "Late-deploy"; `specs/evals-retrieval.md` |
| 4 | **§2 GO/NO-GO** VOID "public demo loads logged-out" at Sat gate → **local `APP_MODE=demo` rehearsal green** (fresh session-isolated state, resettable seed, RSS within limits). Live-URL check moves to Mon Sep 7 after deploy. NO-GO ⇒ submit v1 **Monday**. | §2 safety track | CHECKLIST §F; this section |
| 5 | **Stacked-review protocol:** oldest-first ~10 PRs into `v2`; rebase-merge only; restack after merge; newer PRs draft; `v2`→`main` blocked until v2 drill green. PR #4 closed history. | — | CHECKLIST §I |
| 6 | **Progress snapshot (2026-09-01 ~22:00):** v1 fallback [Done]; WP00 [Done]; WP01 [Done] PR #5 @ `2249836` (Opus blockers closed, Pavlo queue); WP02 [Done for 90%] PR #6 @ `c8e834e` (WIP until #5 merges); WP03–WP11 not started; v2 build ≈24%; review queue 2/~10 open; 1 of 7 build days consumed. | — | CHECKLIST §F; table row "Progress snapshot" |
| 7 | **Product §12.2 canary gate** VOID per addendum §2 — Sep 2 gate is local `APP_MODE=demo` + SQLite tests; live canary Mon Sep 7. `specs/product.md` §12.2 row still says “Public canary loads” (product SOT freeze). | §8 Sep 2 row; product §12.2–12.3 | this section; `specs/data-model.md` contradiction 18 |
| 8 | **`LLM_MAX_OUTPUT_TOKENS`:** canonical default **800** (editions, product §7.2, `.env.example`). Paths/roadmap routes may request up to **1600** (provider ceiling, not env default). | provider.md `max_tokens` line | `specs/editions.md`; `specs/provider.md`; contradiction 16 |
| 9 | **`extra=forbid` + `raw_json`:** v2 API/provider models forbid extras; v1 ingest `homelib_core` keeps `extra=allow`. RightsManifest/ConnectorHit already aligned. | core-models invariant 1 (scoped) | `specs/core-models.md`; contradiction 17 |
| **Public-snapshot readiness (PR-B)** | 2026-09-06 `docs/public-snapshot-readiness`. Licence swapped to PolyForm Noncommercial 1.0.0 (code) + CC BY-NC-SA 4.0 (docs) per ADR-007; ingest User-Agent carries no personal contact; Streamlit secrets, design-sync state and local working notes git-ignored and untracked; internal host / path / run-id references redacted from tracked docs; README gained prerequisites, SQLite-only run, eval and monitoring steps; `docs/data-sources.md` added. graph-refresh now strips the runner workdir from every `graphify-out/*.json` before committing (the committed graph carried the absolute runner path in 859 `source_file` values; fixed on the first post-merge refresh, so publish only after the bot's refresh commit lands). | `uv run pytest tests/test_repo_hygiene.py -q -k relativises` → passes, fails with the strip loop removed; `uv run pytest tests/test_licence.py tests/test_public_docs.py tests/test_data_sources_doc.py apps/ingest/tests/test_user_agent.py -q` → 10 passed (the hygiene tests grep every tracked public doc for the internal-host ban list and for links to the untracked working-notes directory); `just ci` → 779 passed, coverage 91.02% | this row |
| **Course-alignment evals + tracing (PR-C)** | 2026-09-07 `feat/course-alignment-evals`, stacked on PR-B. Closes every item of `docs/zoomcamp-2026-gap-report.md` §FIXES: book-level hit-rate, RRF k sweep, answer-cosine second LLM eval, chunking experiment, agent loop on the Mentor path, USD cost, online judge, OpenTelemetry tracing + `GET /v1/traces/{id}`, demo answer cache, singleton/speed pin. | **Evals (SQLite seed, 235 q):** book hit@5 lexical 0.077 / vector 0.906 / hybrid 0.906 / hybrid_rerank 0.906; RRF k ∈ {1,10,60,100,200} → 0.638 / 0.906 / 0.483 at every k (flat — lists barely overlap, ADR-001); answer cosine (Ollama, 10 q × 4 arms) stepwise 0.534 > production 0.515 > concise 0.480 > cited_first 0.441; chunking (6/18 books, 73 q) vector hit@5(book) 0.973 / 0.986 / 0.973 at 600/1200/2000 chars, top-5 tokens 422 / 893 / 1,560 vs 3,073 whole-section. **Live gate (local API, SQLite seed, Ollama qwen2.5:7b-instruct, priced with the Groq example rates):** selfhosted ask → `hybrid_rerank`, 1 citation, 50.4 s, cost_usd 0.00097, trace tree `retrieve 10.9 s / llm 39.4 s / cite 0.04 ms`; after the rerank-span fix `retrieve 11.1 s ⊃ rerank 5.3 s (cold)`. Demo mode: first ask 9.3 s live, identical repeat `cache_hit: true` in 3 ms (56 ms wall), degraded attempt not cached; `python -m scripts.judge_recent --n 5` judged 4 live rows (relevance 3, 3, 4, 5) and the `judged_relevance` chart showed them; Observatory 9 charts. Warm retrieval+rerank 278 ms vs 12.5 s cold (`test_models_load_once_per_process`). `just ci` → 861 passed, coverage 90.08% (floor 90%) | this row |

| **Cloud demo live (bonus #11)** | 2026-09-08 | Streamlit Community Cloud at https://homelib.streamlit.app/ serving Forgejo tip mirrored to GitHub `main`; CHECKLIST rubric row 11 marked done. Live-demo audit follow-ups tracked on `fix/live-demo-audit`. | Manage app URL loads; doors render; evidence for ask cite rate lands after Cloud-log-driven P0 fix | this row |
