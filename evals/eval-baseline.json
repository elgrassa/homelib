{
  "metrics": {
    "hybrid_rerank.hit_rate_at_5": 0.70,
    "hybrid_rerank.mrr_at_5": 0.55,
    "judge.mean_faithfulness": 3.8
  },
  "specs": {
    "hybrid_rerank.hit_rate_at_5": {
      "key": "hybrid_rerank.hit_rate_at_5",
      "direction": "higher_is_better",
      "margin": 0.03
    },
    "hybrid_rerank.mrr_at_5": {
      "key": "hybrid_rerank.mrr_at_5",
      "direction": "higher_is_better",
      "margin": 0.03
    },
    "judge.mean_faithfulness": {
      "key": "judge.mean_faithfulness",
      "direction": "higher_is_better",
      "margin": 0.15
    }
  },
  "notes": {
    "hybrid_rerank.hit_rate_at_5": "PLACEHOLDER floor, not yet measured — evals/ground_truth.jsonl and evals/retrieval_eval.py (WP-12/13) do not exist yet. 0.70 is a conservative guess for the RRF+cross-encoder arm, based on typical hit-rate@5 figures reported for hybrid retrieval over a few-hundred-question set on a 15-25 book shelf. Margin 0.03 is set to absorb LLM ground-truth generation noise between reruns (question phrasing varies run to run), not real regressions. REPLACE both the number and this note with the measured value plus its real justification once the WP-12/13 retrieval eval runs for the first time.",
    "hybrid_rerank.mrr_at_5": "PLACEHOLDER floor, same caveat as hybrid_rerank.hit_rate_at_5 above — no real retrieval eval run exists yet. 0.55 assumes that once cross-encoder rerank is applied, most hits land within the first 1-2 ranks rather than deep in the top-5. Margin 0.03 mirrors hit_rate_at_5's tolerance for ground-truth-generation noise between runs. REPLACE with the measured value once WP-12/13 land.",
    "judge.mean_faithfulness": "PLACEHOLDER floor for the WP-15 LLM-judge faithfulness score (1-5 scale) pending a real evals/results run — no judge pipeline has executed yet. 3.8 is a deliberately unambitious guess so the gate is not immediately red on the very first real measurement. Margin 0.15 anticipates judge-model variance and mirrors the illustrative example in specs/eval-gate.md; RE-DERIVE it properly by rerunning the same prompt/model variant 3x and observing the spread, once the judge pipeline exists."
  }
}
